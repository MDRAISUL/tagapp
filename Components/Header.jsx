import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-web'

const Header = () => {
    return (
        <SafeAreaView>
            <Text>It's Header</Text>
        </SafeAreaView>
    )
}

export default Header
